/*
const defaultOptions =
	{
		appenders: {
			console: {
				type: 'console',
				layout: {
					type: 'pattern',
					pattern: '%[%d{yyyy-MM-dd hh:mm:ss:SSS} pid:%x{pid} %p %c -%] %m',
					tokens: {
						pid: function () {
							return process.pid;
						}
					}
				}
			},
			dateLog: {
				type: 'dateFile', filename: "logs/web",
				pattern: "-yyyy-MM-dd.log", alwaysIncludePattern: true,
				pollInterval: 1,
				layout: {
					type: 'pattern',
					pattern: '[%d{yyyy-MM-dd hh:mm:ss:SSS}] [%p]:%m'
				}
			}
		},
		categories: {
			default: {appenders: ['console'], level: 'debug'},
			dateLog: {appenders: ['dateLog'], level: 'debug'}
		},
		pm2: true,
		pm2InstanceVar: 'INSTANCE_ID',
		replaceConsole: true
	};
 */

var log4js = require("../index");
var Logger = (new log4js({appenders:{file:{filename: __dirname +"/logs/net.log"}}},'file')); // appenders:{dateLog:{filename: "logs/net"}}
var logger = Logger.logger;
function foo() {
	
	logger.info(Logger.location() + "info abcd");
	console.log(Logger.location() + "mmmmm");
}
foo();